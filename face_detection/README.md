## Setup

1. **Install dependencies:**
```bash
pip install -r requirements.txt
```

2. **Enroll your face:**
```bash
python enroll_face.py
```

3. **Recognize faces:**
put your faces in the `images/` and run the following command to test the recognition:
```bash
python recognize_faces.py
```
check the output in the `output/`.